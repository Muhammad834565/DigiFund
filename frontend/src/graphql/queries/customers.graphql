query GetAllCustomers {
  customers {
    id
    name
    email
    phone
    address
    createdAt
  }
}

query GetCustomer($id: String!) {
  customer(id: $id) {
    id
    name
    email
    phone
    address
    createdAt
  }
}
